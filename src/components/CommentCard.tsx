import {PostType} from '../utils/types';
import CustomText from './CustomText';

const CommentCard = ({item}: {item: PostType}) => {
  return <CustomText text="Comment card" />;
};


export default CommentCard;
